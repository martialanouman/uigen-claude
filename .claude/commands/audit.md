Your goal is to update any vulnerable dependencies in the project.

Do the following:
1. Run `npm audit` to find vulnerable packages.
2. Run `npm audit fix` to update vulnerable packages.
3. Run tests to ensure everything is working as expected.
